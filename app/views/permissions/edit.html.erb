<% content_for :menu do -%>
  <h1>Editando permissões de <%= @user.name %> </h1>
  <%= mount_menu %w[save show] %>
<% end -%>

<% form_for :permissions, :url => '/permissions/update' do |f| %>
  <div class="form-wrapper">
    <%= f.error_messages %>
    <%= hidden_field :user, :id %>

    <% @controllers.each do |i| %>
      <%= perm_options(i.keys[0].to_s, i.values[0], @user) %>
    <% end %>
  </div>
<% end %>

