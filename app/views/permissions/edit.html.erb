<h1>Editando permissões de <%= @user.name %> </h1>

<% form_for :permissions, :url => '/permissions/update' do |f| %>

  <%= mount_menu %>

  <%= f.error_messages %>

  <%= hidden_field :user, :id %>

  <% @controllers.each do |cont, nick| %>
  <p>
    <%= perm_options(cont, nick, @user) %>
  </p>
  <% end %>
<% end %>

