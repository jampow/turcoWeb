<h1>Editando Usuário</h1>

<% form_for(@user) do |f| %>
  <%= f.error_messages %>

  <%= mount_menu %>

  <p>
    <%= f.label :name, "Nome: " + f.text_field(:name)%>
  </p>
  <p>
    <%= f.label :login, "Login: " + f.text_field(:login)%>
  </p>
  <p>
    <%= f.label :email, "E-Mail: " + f.text_field(:email)%>
  </p>
  <p>
    <%= f.label :password, "Senha: " + f.password_field(:password)%>
  </p>
  <p>
    <%= f.label :password_confirmation, "Confirme a senha: " + f.password_field(:password_confirmation)%>
  </p>
<% end %>

